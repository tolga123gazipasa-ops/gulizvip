{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo 'Python stdlib only, no deps needed'"
  },
  "deploy": {
    "startCommand": "python server.py",
    "healthcheckPath": "/api/flights",
    "healthcheckTimeout": 30,
    "restartPolicyType": "always"
  }
}
